import { useParams } from "react-router-dom";
import { orders } from "../../data";
export default function OrderTracking(){const {orderId}=useParams(); const order=orders.find(o=>o.id===orderId)||JSON.parse(localStorage.getItem("lastOrder")||"null"); return <main style={{maxWidth:800,margin:"0 auto",padding:"50px 20px",color:"#1A1A1A"}}><h1 style={{color:"#1A2E1A"}}>Track Order</h1>{order?<div style={{background:"#FFFFFF",border:"1px solid #D6E8D6",borderRadius:8,padding:20}}><h2 style={{color:"#4CAF50"}}>{order.id}</h2><p>Status: {order.orderStatus||"Placed"}</p><p>Total: ₹{order.total}</p></div>:<p>Order not found.</p>}</main>}
