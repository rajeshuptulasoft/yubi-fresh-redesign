import { useMemo, useState } from "react";
import { products } from "../../data";
import { ProductCard, TruckAnimation, colors, heading } from "./HomeSections";
import { useWindowSize } from "../../hooks/useWindowSize";
export default function SpiceStore(){const [mode,setMode]=useState("single"); const [q,setQ]=useState(""); const {width}=useWindowSize(); const items=useMemo(()=>products.filter(p=>p.category==="spices"&&p.name.toLowerCase().includes(q.toLowerCase())),[q]); return <main style={{maxWidth:1280,margin:"0 auto",padding:width<=768?"24px 16px":"60px 40px",color:colors.text}}><div style={{display:"flex",alignItems:"center",justifyContent:"space-between",gap:20,marginBottom:18}}><div><h1 style={{...heading,margin:0}}>Spice Store</h1><p style={{color:colors.text}}>Single-origin, freshly ground, world-class</p></div><TruckAnimation/></div><div style={{display:"flex",justifyContent:"flex-end",marginBottom:16}}><div style={{display:"flex",padding:4,border:"1px solid #D6E8D6",borderRadius:12}}>{["single","bulk"].map(m=><button key={m} onClick={()=>setMode(m)} style={{padding:"10px 18px",border:"none",borderRadius:8,background:mode===m?"#4CAF50":"#FFFFFF",color:mode===m?"#FFFFFF":"#1A1A1A",fontWeight:800}}>{m==="single"?"Single Order":"Bulk Order"}</button>)}</div></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search spices..." style={{width:"100%",padding:13,border:"1px solid #D6E8D6",borderRadius:8,color:"#1A1A1A",marginBottom:20}}/><div style={{display:"grid",gridTemplateColumns:width<=768?"1fr":width<=1024?"repeat(2,1fr)":"repeat(4,1fr)",gap:18}}>{items.map(p=><ProductCard key={p.id} product={{...p,price:mode==="bulk"?(p.bulkPrice||p.price):p.price}}/>)}</div></main>}
