import { useState } from "react";
import { users } from "../../data";
import { title } from "./AdminDashboard";
export default function AdminUsers() { const [q,setQ]=useState(""); const rows=users.filter(u=>`${u.name} ${u.email}`.toLowerCase().includes(q.toLowerCase())); return <div><h1 style={{...title, fontFamily: "'Plus Jakarta Sans', 'DM Sans', sans-serif"}}>User Data</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by name or email" style={input}/><Table headers={["User ID","Name","Email","Phone","Total Orders","Joined Date"]} rows={rows.map(u=>[u.id,u.name,u.email,u.phone,u.totalOrders,u.joinedAt])}/></div> }
export function Table({headers,rows}){const [selected,setSelected]=useState(null);return <><div className="admin-table-wrap"><table className="admin-table"><thead><tr>{[...headers,"Action"].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j} data-label={headers[j]}>{c}</td>)}<td data-label="Action"><button onClick={()=>setSelected(r)} className="admin-view-btn">View</button></td></tr>)}</tbody></table></div>{selected&&<div className="admin-modal-backdrop" onClick={()=>setSelected(null)}><div className="admin-modal" onClick={(event)=>event.stopPropagation()}><div className="admin-modal__head"><h3 className="admin-modal__title">Information</h3></div><div className="admin-modal__body">{headers.map((h,idx)=><div key={h} className="admin-modal__row"><span className="admin-modal__label">{h}</span><span className="admin-modal__value">{selected[idx]}</span></div>)}</div><div className="admin-modal__foot"><button onClick={()=>setSelected(null)} className="admin-modal__close">Close</button></div></div></div>}</>}
const input={width:"100%",maxWidth:420,padding:12,border:"1px solid #D6E8D6",borderRadius:8,color:"#1A1A1A"};
